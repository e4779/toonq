# TODO.md — toonq

## Этап 4: JSONL + Truncate

- [ ] `--slurp` / `-s` — чтение JSONL как массива
- [ ] `--truncate N` — обрезка строковых полей
- [ ] Авто-детект JSONL при ошибке парсинга JSON
- [ ] Тесты на сессионных файлах
